spring:
  profiles:
    active: ${APP_PROFILE:dev}
  jpa:
    open-in-view: false

cors:
  origins: ${CORS_ORIGINS:http://localhost:5173,http://localhost:3000}

springdoc:
  api-docs:
    path: /api-docs
  packagesToScan: com.br.tcc.bfn.controllers